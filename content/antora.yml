name: modules
title: Demystifying AAP on OCP
version: master
nav:
  - modules/ROOT/nav.adoc

asciidoc:
  attributes:
    page-pagination: true
    lab_name: "Demystifying AAP on OCP"
    # The following attributes are injected at deploy time from the showroom
    # user_data.yml configmap (populated by the RHDP provisioning platform):
    #   password, user, guid, openshift_console_url, openshift_cluster_ingress_domain
    #
    # Dev-mode placeholder values for local Antora builds:
    password: "changeme"
    user: "user-guid"
    guid: "guid"
    # alia_model is hardcoded since it is not injected at deploy time
    alia_model: "granite-3-2-8b-instruct"
    # ALIA connection details — injected at deploy time from ocp4_workload_showroom_user_data_seed
    # Dev-mode placeholder values for local Antora builds:
    alia_url: "https://litellm-prod.apps.maas.redhatworkshops.io/v1"
    alia_token: "token-not-set"
