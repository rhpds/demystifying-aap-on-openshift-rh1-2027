= Scenario 4: Execution Environment credentials

[cols="3",options="header"]
|===
| Difficulty | Components | Break Type
| Easy | Automation Controller | Execution Environment / Credentials
|===

== Objectives

* Log in to the *AAP Gateway UI* successfully.
* Launch the available job templates and identify the one that fails.
* Diagnose the root cause.
* Resolve the underlying issue.

[#credentials]
== Your credentials

Use the following credentials to log into AAP via the tab on the right:

[cols="1,2",options="header"]
|===
| Field | Value
| *Username* | admin
| *Password* | {password}
|===

[#ocp-console]
== OpenShift Console

Use the following credentials to log into OpenShift via the tab on the right:

[cols="1,2",options="header"]
|===
| Field | Value
| *Username* | {user}
| *Password* | {password}
|===

== Troubleshooting

=== Step 1: Run the Break Scenario job

Log into AAP using the *Ansible Automation Platform* tab. Navigate to *Automation Execution → Templates* and launch the *Break Scenario* job template. Wait for it to complete before proceeding.

=== Step 2: Launch job templates and identify the failure

In the AAP UI, navigate to *Automation Execution → Templates*. Launch the available job templates one by one and observe which one fails.

Review the job output to understand the error message.

=== Step 3: Inspect the failed job

Click into the failed job run and review its output in detail. The error will indicate a failure.

For greater detail, check the *OpenShift Console → Workloads → Pods* to find the automation job pod and review its logs and events.

=== Step 4: Fix the issue

Edit the failing job template in the AAP UI to correct the configuration.

Refer to the solution page if needed: xref:scenario-4-solution.adoc[Solution: Execution Environment Credentials]

=== Step 5: Verify

Re-launch the previously failing job template and confirm it now runs successfully.

== Hint

The failure is in how the job template is configured, specifically something related to the Execution Environment or its associated credentials. The job and pod logs will give you a clear error message to work from.

== Check Your Work

++++
<div class="validate-button-placeholder" data-module="scenario-4"></div>
<div class="solve-button-placeholder" data-module="scenario-4"></div>
++++