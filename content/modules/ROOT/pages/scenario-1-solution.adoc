= 💡 Solution: Scenario 1 - Automation Hub storage issue

This page provides the detailed solution for Scenario 1. Review these steps after you have attempted the diagnosis and resolution yourself.

---

== 🛑 Problem: Missing ReadWriteMany (RWX) storage class

=== Diagnosis

The Automation Hub pods were failing due to an inability to provision or mount the required *Persistent Volume Claim (PVC)*. In a typical HA deployment, multiple Hub pods need to access the same storage simultaneously, which requires a storage type that supports the *ReadWriteMany (RWX)* access mode.

The original StorageClass was set to a type that only supports *ReadWriteOnce (RWO)*, causing PVC binding and pod scheduling failures.

=== 🛠️ Resolution: Updating the storage class

The fix involves modifying the main *Ansible Automation Platform Custom Resource (CR)* to specify a valid RWX-capable StorageClass, followed by manually forcing a clean reconciliation.

==== 1. Modify the AAP custom resource

If you don't specify a storage class, the operator will use the cluster default storage class. In this case the default storage class is RWO storage; Automation Hub requires RWX storage.

In the *OpenShift Console*:

. Navigate to *Ecosystem → Installed Operators* and select your namespace from the dropdown.
. Click on *Ansible Automation Platform* in the operator list.
. Click the *Ansible Automation Platform* tab to see your CR instances.
. Click on your AAP instance (named `{user}-aap`).
. Switch to the *YAML* tab and update the `file_storage_storage_class` field in the Hub section:

[source,yaml]
----
# Example snippet of the modified AAP CR
spec:
  hub:
    file_storage_storage_class: ocs-storagecluster-cephfs
----

==== 2. Delete the Automation Hub CR

The current Hub deployment is holding on to old information and needs to be deleted so the Operator can redeploy it cleanly.

In the *OpenShift Console*:

. Navigate to *Ecosystem → Installed Operators* and select your namespace from the dropdown.
. Click on *Ansible Automation Platform* in the operator list.
. Click the *Automation Hub* tab to see your CR instances.
. click the three-dot menu on the right, and select *Delete AutomationHub*.

==== 3. Delete the old PVC

The existing PVC created with the incorrect StorageClass must be deleted so the Operator can provision a new one with the correct StorageClass.

In the *OpenShift Console*, navigate to *Storage → PersistentVolumeClaims* and select your namespace from the dropdown. Find the Hub file storage PVC, click the three-dot menu on the right, and select *Delete PersistentVolumeClaim*.


==== 4. Force reconciliation

Manually delete the Gateway and Hub Operator manager pods to trigger the Operator to recognize the change and provision the new PVC correctly.

In the *OpenShift Console*, navigate to *Workloads → Pods* and select your namespace. Find the Gateway and Hub Operator manager pods (named `aap-gateway-operator-controller-manager-...` and `automation-hub-operator-controller-manager-...`), click the three-dot menu on the right, and select *Delete Pod*.

==== 5. Verification

Monitor *Workloads → Pods*. The Operator will provision a new hub deployment, create a new PVC and redeploy Automation Hub. The Hub pods will successfully mount the volume and transition to a `Running` and `Ready` state.
