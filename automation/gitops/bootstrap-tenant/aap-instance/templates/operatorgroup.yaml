---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ .Values.tenant.username }}-aap-operatorgroup
  namespace: {{ .Values.tenant.namespace }}
spec:
  targetNamespaces:
    - {{ .Values.tenant.namespace }}
