apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.tenant.username }}-aap-admin-password
  namespace: {{ .Values.tenant.namespace }}
type: Opaque
stringData:
  password: {{ .Values.tenant.password }}
