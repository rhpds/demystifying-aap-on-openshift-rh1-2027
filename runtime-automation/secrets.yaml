# =============================================================================
# Runtime Automation Secrets
# =============================================================================
#
# This file should be encrypted with Ansible Vault:
#   ansible-vault encrypt secrets.yaml
#
# The VAULT_PASSWORD environment variable is automatically injected by Showroom
# and decrypts this file when main.yml loads it.
#
# For now, this is a placeholder. Variables defined here override defaults in main.yml.
# =============================================================================

# AAP Controller credentials (if different from defaults)
# aap_admin_password: !vault |
#   $ANSIBLE_VAULT;1.2;AES256
#   ...

# Additional lab-specific secrets can go here
